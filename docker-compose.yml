version: '3.8'

services:
  db:
    image: mongo:6.0
    container_name: autoclaw_db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - autoclaw_mongo_data:/data/db
    networks:
      - autoclaw_net

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autoclaw_api
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - MONGO_URI=${MONGO_URI}
      - USER_EMAIL=${USER_EMAIL}
      - USER_PASSWORD=${USER_PASSWORD}
    depends_on:
      - db
    networks:
      - autoclaw_net
    volumes:
      - gogcli_data:/root/.config/gogcli

  web:
    build:
      context: .
      dockerfile: ./infra/docker/Dockerfile.web
    container_name: autoclaw_web
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=${VITE_API_URL}
    depends_on:
      - api
    networks:
      - autoclaw_net

volumes:
  autoclaw_mongo_data:
  gogcli_data:

networks:
  autoclaw_net:
    driver: bridge
